export default class Persion {
  /**
   * @param {object} persion 输入的对象
   * @param {string} persion.name 输入对象的名称
   * @returns {object}
   * @property {string} name 输出名称
   */
  getName(persion){
    return persion;
  }
}